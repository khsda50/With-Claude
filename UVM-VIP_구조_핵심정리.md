# UVM VIP 구조 — 핵심 정리

Q-Channel responder agent를 설계하며 정리한 것. **프로토콜과 무관하게 적용되는 원리**만 남기고,
Q-Channel은 예시로만 쓴다. 상세 설계는 `VIP_Q-Channel_seq-item설계.md`.

---

## 1. 소유 모델 — 이게 뼈대다

| 컴포넌트 | 소유하는 것 | 한 문장 |
|---|---|---|
| **seq_item** | **어휘** | 무엇을 말할 수 있는가 |
| **sequence** | **의도** | 무엇을, 어떤 순서로 말할 것인가 |
| **driver** | **시간과 규칙** | 그 말을 인터페이스 위에 어떻게 펼치는가 |
| **monitor** | **사실** | 실제로 무슨 일이 일어났는가 |
| **SVA** | **감시** | 규칙이 깨졌는가 |
| *DUT* | *(responder 한정)* **타이밍** | 대화가 언제 시작되는가 |

프로토콜이 뭐든 이 표는 안 바뀐다. 바뀌는 건 item에 뭐가 담기느냐뿐이다.

**monitor를 driver와 분리하는 이유**: driver는 자기가 *의도한 것*을 알 뿐이다. driver에 버그가 있으면
driver의 보고는 증거가 못 된다. monitor는 인터페이스만 보므로 driver와 독립적이다.
→ **driver를 믿지 말고 SVA로 감시한다.** (SVA는 선택이 아니라 필수)

---

## 2. seq_item — 무엇을 담는가

> **seq_item = (이 인터페이스에서 내가 고를 수 있는 선택지) + (이 인터페이스에서 일어난 사실)**

**함정**: "흔들 것"을 **신호**로 읽으면 핀 묶음으로 퇴행한다.

| | |
|---|---|
| "수락할까 거절할까" | **선택지** ✓ |
| "QACCEPTn을 0으로 할까" | 선택지처럼 보이지만 **실행 방법** ✗ |

**필터**: 그 필드가 **검증하려는 질문 하나에 대응하는가?**

**작업 순서 — 필드가 아니라 질문을 먼저 적는다**

| 검증 질문 | 흔들 것 | 관찰할 것 |
|---|---|---|
| 거절하면 CRMU가 재시도하나 | `response=DENY` | 다음 요청 발생 |
| 응답이 늦으면 타임아웃 처리하나 | `resp_latency` 큰 값 | 요청 유지 여부 |
| 수락 후 클럭을 정말 끄나 | `response=ACCEPT` | 클럭 출력 |

왼쪽 = 검증 계획 / 가운데 = item 필드 / 오른쪽 = scoreboard·coverage

**경계**: item에 담는 관찰값은 **이 인터페이스 위의 사실**로 한정. 클럭 출력 같은 건 다른 monitor 몫.
안 그으면 item이 온갖 걸 들고 다니며 부푼다.

---

## 3. 추상화 = 동사를 감추는 것

item에 동사를 나열하는 게 아니다. **동사가 사라지게** 하는 것이다.

```systemverilog
// 핀 묶음 — 시퀀스가 4가지를 해야 함
wait (vif.QREQn == 0);        // 기다린다
item.qacceptn = 0;            // 값을 만든다
...                           // 유지한다 / 되돌린다

// 의미 — 시퀀스가 1가지만 함
item.response = QCH_ACCEPT;   // "이번엔 수락"
```

셋이 사라졌다. 사라진 게 아니라 **driver가 가져갔다.**

**driver는 번역기가 아니라 전개기(unfolding)다.**
item은 시간이 없는 문장이고, driver가 그 문장을 시간 위에 펼친다.
값 변환은 driver 코드의 소수다 — 대부분은 타이밍과 관찰이다.

**필드가 핀 이름의 나열이면 추상화한 게 아니라 옮겨 적은 것이다.**
표준 프로토콜은 둘이 거의 일치해 티가 안 나지만, custom IP는 정확히 여기서 갈린다.

---

## 4. 입도 — 어디에 선을 긋는가

> **테스트에게 의미 있는 가장 작은 단위이면서,
> driver가 외부 문맥 없이 실행할 수 있을 만큼 완결된 것.**

두 조건이 반대로 당긴다. 그 균형점이 설계다.

| 층위 | 판정 |
|---|---|
| 신호 이벤트 1개 | 낮음 — 프로토콜 지식이 시퀀스로 샌다 |
| **주고받음 1회** | **적정** |
| 시나리오 전체 | 높음 — 이건 sequence의 일. 배열 필드가 생기면 신호 |

**상한 신호**: 필드에 배열이 생긴다 = 트랜잭션이 아니라 컨테이너다.
사용자마다 원하는 시나리오가 달라 필드가 무한히 자란다.

---

## 5. 이 결정이 파급되는 네 곳

seq_item이 **가장 레버리지가 큰 결정**인 이유 — 한 번 정하면 넷이 따라온다.

- **시퀀스** — 두 줄로 짜이거나, 프로토콜을 다 알아야 하거나
- **coverage** — `coverpoint item.response` 한 줄이거나, 문맥 재구성이 필요하거나
- **scoreboard** — item 단위 비교가 되거나, 안 되거나
- **로그** — 읽히거나, 파형과 다를 바 없거나

그리고 되돌리기가 가장 비싸다(전부가 이 타입에 의존).

> **item이 표현하지 못하는 현상은 검증이 볼 수 없다.**
> 필드가 아닌 것은 randomize 대상이 아니고, coverpoint로 가리킬 수도 없다.
> **item 설계 = 탐색 공간의 경계 = coverage 어휘 설계.**

---

## 6. 구조 결정 — 판단 기준

**① agent 방향** — item 구조를 결정한다
- DUT가 요청하는 쪽이면 **responder**(반응형), 우리가 거는 쪽이면 **initiator**
- responder에서는 **"언제 대화가 시작되는가"에 대한 권한이 없다**
  → 시퀀스는 답을 미리 쌓아두고, driver가 요청이 올 때마다 꺼내 쓴다
  → 타임아웃/abort 처리가 필수

**② drive item과 monitor item 분리**
- responder에서 요청 시각·간격은 **DUT 소유**라 rand가 아니다
- 자극 필드(rand)와 관찰 필드(non-rand)를 한 클래스에 섞지 않는다

**③ 방향별 item 통합 vs 분리**

| | |
|---|---|
| **payload가 있다** (AXI: addr/data) | **통합** — 공통 필드가 실체이고, 양쪽이 같은 사물을 본다 |
| **순수 핸드셰이크** (Q-Ch) | **분리** — 각 측이 고를 수 있는 것에 교집합이 없다 |

단, **monitor item은 언제나 통합.** 방향과 무관하게 "일어난 일"을 보고하므로.
그러면 scoreboard 매칭 문제도 자동 해결된다.

**④ 신호를 필드로 둘 것인가 — 제어냐 기록이냐**

| | 판정 |
|---|---|
| `rand bit qacceptn` — **제어** | ✗ 핀 묶음으로 퇴행 |
| `bit observed_qacceptn` — **기록** | ○ 무해 (`UVM_NOCOMPARE` 필수) |

전체 스냅샷은 손해다 — 파형이 이미 완벽히 갖고 있다.
디버그에 필요한 건 신호 값이 아니라 **"파형의 어디를 볼지"** = 시각과 상태 추적.
예외: 회귀에서 dump를 끄는 경우 → 그때도 **결정적 순간의 몇 개**만.

> **신호를 안 넣은 게 아니라, 제어와 기록을 분리한 것.**

**⑤ 에러 주입도 의미 레벨**
- 실무 주력은 **item의 violation enum**(`kind`). `force`는 최후의 수단
- 이유: 재현성 / 로깅 / coverage / randomize — `force`는 이 넷을 다 잃는다
- **오직 driver의 negative-path 분기만 규칙을 깨는 법을 안다**
- 기본 회귀에서 config로 분리 (안 그러면 진짜 버그와 의도된 위반이 섞인다)
- 진짜 어려운 건 만드는 게 아니라 **"그때 DUT가 뭘 해야 하는지"를 스펙에 정의**하는 것.
  그리고 검증 가치는 위반 자체보다 **복구**에 있다

**⑥ coverage sampling rate는 하나가 아니다**

| covergroup | sampling 시점 |
|---|---|
| 트랜잭션 단위 | 한 사이클 완료 시 |
| **상태 전이 단위** | **상태가 바뀔 때마다** ← transition bin은 이게 없으면 안 모인다 |

---

## 7. 상용 VIP는 왜 다르게 생겼나

| | 상용 VIP | in-house VIP |
|---|---|---|
| 사용자 | 수천 곳, 모르는 사람 | 우리 팀 |
| 프로토콜 | 표준 (신호 = 트랜잭션) | custom (동작으로 정의) |
| 우선순위 | **무엇이든 만들 수 있을 것** | **빠르게 올바르게 쓸 것** |
| 결과 | 필드 100개, 문서 300쪽 | 필드 5개, 읽으면 앎 |

둘 다 맞다. **전제가 다를 뿐.**

> 상용 VIP는 **모든 미래**를 미리 사둬야 하고,
> in-house VIP는 **필요한 미래만** 그때 사면 된다.
> (고객은 소스를 못 고치지만, 우리는 우리 코드다)

그리고 필드가 100개여도 사용자는 3개만 쓴다 — **기본값이 추상화를 대신**한다.
"평소엔 추상적으로, 필요할 때 핀 레벨로"를 한 클래스에 합친 구조.

---

## 8. 표준 프로토콜과 custom IP의 진짜 차이

추상화의 **크기**가 아니다. AXI write 하나도 3개 채널과 6번의 악수를 감춘다.

> **차이는 그 경계를 누가 그었느냐다.**

| | AXI | Q-Channel |
|---|---|---|
| 스펙이 트랜잭션을 명명하는가 | ✓ "a write transaction" | ✗ 상태 6개와 신호 4개뿐 |
| 그래서 하는 일 | **구현** | **설계**부터 |

표준이라고 다 명명해주는 건 아니다 — Q-Channel은 AMBA 표준이지만 상태 머신만 정의한다.

→ JD의 `신규 Protocol 또는 Custom IP에 대한 UVM Agent **설계/구현** 역량`에서
**설계와 구현을 나란히 쓴 게 우연이 아니다.**

---

## 9. 외워둘 한 줄

1. **item = 어휘 / sequence = 의도 / driver = 시간과 규칙 / monitor = 사실**
2. **item이 표현 못 하는 현상은 검증이 볼 수 없다**
3. **잘 된 추상화의 신호는 동사가 줄어드는 것**
4. **필드가 핀 이름의 나열이면 옮겨 적은 것**
5. **item의 추상화 수준 = 이 VIP를 쓰는 사람이 알아야 하는 지식의 양**
6. **item 설계와 coverage 설계는 같은 결정을 두 번 하는 것**
7. **driver를 믿지 말고 SVA로 감시한다**
8. **핸드셰이크는 다 같은 모양이다** — 하나를 제대로 설계하면 나머지가 따라온다
