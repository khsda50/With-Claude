# 지원서 작성 초안

근거 자료: `경력_원장.md` / `SKhynix_지원서_양식.md` / `../JD_HBM-Digital-Design-검증.md` /
`../UVM-VIP_구조_핵심정리.md`

> **관통 프레임: "하나의 능력 × 프로토콜 여러 개".**
> 업무 종류를 나열하는 대신 — *표준 VIP가 없는 IP를 검증 가능하게 만드는 일* 한 가지를
> GMAC · USB 2.0 · 고객사 AMBA VIP · Q-Channel에서 반복했다는 축으로 전체를 세웠습니다.
> "여러 가지 해봤다"(breadth)는 3회 탈락한 프레임이라 쓰지 않았습니다.
>
> **`[TODO]` 는 사용자님만 아는 값입니다.** 지어내면 그대로 제출되는 서류라 비웠습니다.
> 글자수는 전부 실측값입니다.

---

## A. 경력 섹션

### 폼 입력 필드

| 항목 | 필수 | 값 |
|---|---|---|
| 회사명 | * | 가온칩스 |
| 부서명 | * | **[TODO]** |
| 직무 | * | SoC Design / Design Verification |
| 최종직위 | * | 선임 |
| 재직여부 | * | 재직중 |
| 급여 | * | **[TODO]** 만원 |
| 근무시작년월 | * | 2021-11 |
| 근무종료년월 | * | 재직중 — 입력 방식 **확인 필요** |

### 담당업무 (500자) — 확정안 v8

```text
[IP 통합·서브시스템 검증]
- Cortex-A55 CPU 서브시스템 UVM 검증(DSU 기능 분석 기반 테스트케이스, 시퀀스·FW 기반 검증)
  및 C2C Top 통합 검증 — 진행 중
- Ethernet GMAC 통합·연동 검증: UVM 환경·케이스·스코어보드 개발
- Lidar 서브시스템 통합·검증, Mask ROM 부트 검증
- CMN-700·FlexNoC 데이터패스 검증

[검증 환경·VIP]
- Q/P-Channel 검증을 일원화한 In-house UVM VIP 개발(agent·커버리지 모델)
- config 기반 버스 검증 환경 자동화 및 스코어보드 설계
- 고객사 SV class AMBA VIP 인수·확장
- 커버리지 수집·DB 병합 플로우 구축
- DPI 기반 Top/서브시스템 sim 환경 구축·Dual-die 개편

[방법론·설계]
- 재사용 UVM 환경 표준화, DTL 기반 구조 및 규칙 수립
- Lidar 서브시스템 설계, IP FE Sign-off
```

- 공백 포함 **495자** → 여유 5자

**v7 에서 고친 것**

- `버스 connectivity 검증 환경 자동화 구성` →
  **`버스 스펙 config 기반 컴포넌트 자동 생성 구조에 스코어보드 설계`**.
  `자동화` 라는 뭉뚱그린 말 대신 **config-driven elaboration** 이라는 메커니즘을
  그대로 쓴다. 아는 사람만 쓸 수 있는 문장이 되고, 동시에 **본인 몫이 스코어보드
  쪽이라는 것도 정확해진다** (프레임워크 전체를 만든 것이 아니다)
- Q/P-Channel VIP 에 `(agent·커버리지 모델)` 추가. 최강 카드가 맨몸 한 줄이었다
- Lidar 표기를 `Lidar integration` / `Lidar RTL 설계` 로 줄여 중복감 완화

**Regression 은 쓰지 않는다**

회귀 환경이나 스크립트를 만든 것이 아니라 돌린 것뿐이므로 쓰지 않는다.
앞선 §8 평가에서 "회귀 흔적이 약하다" 고 적은 것은 **관찰이지 채우라는 뜻이 아니다.**
DTL 줄의 `회귀 구조·규칙 수립` 은 실제로 한 일이므로 그것으로 충분하다.

**VIP 두 항목을 합치지 않는 이유**

| | 합치면 |
|---|---|
| JD 대응 | 담당업무 2번이 `In-house VIP 개발` **및** `3rd Party VIP 도입/활용` 으로 둘을 따로 요구한다. 합치면 한쪽만 대응하는 것처럼 보인다 |
| 신호 강도 | `VIP 개발·확장` 으로 뭉개지며 **In-house 개발이라는 가장 강한 신호가 희석**된다 |
| 대비 | 두 줄이 나란히 있으면 "만들 줄도 알고 남의 구조를 읽을 줄도 안다" 는 대비가 생긴다. 합치면 사라진다 |

합쳐서 자리를 만드는 대신 **다른 항목에서 줄여 Q/P-Channel VIP 를 두껍게** 했다.
`DPI 기반 sim 환경` 과 `Lidar` 표기에서 덜어냈다.

---

## B. 자기소개서 1 — 지원동기 / 향후 계획 / 퇴사 동기 (742자)  ★축 교체

### 축: "만드는 사람" → "무엇을 검증해야 하는지 아는 사람"

이전 판은 *없는 것을 만들어 온 사람* 을 축으로 세웠다. 사실이지만 **비중이 실제와
달랐다.** 4년의 주력은 IP 를 붙여 시스템으로 돌리고 그 사이에서 검증 안 된 지점을 찾아
케이스를 만들고 디버깅한 일이다. 그 축으로 서면 면접이 곧바로 *In-house VIP 몇 개,
커버리지 클로저는* 으로 들어온다 — **가장 약한 땅에서 싸우겠다고 선언하는 셈이다.**

IP DV 라도 그 IP 가 시스템에서 어떻게 쓰이는지 모르면 **무엇을 검증해야 할지 정할 수
없다.** 검증 항목을 정하는 능력이 시스템 이해에서 나온다. 이것이 SoC DV 경력을 IP DV
자리에서 강점으로 번역하는 정직한 논리이고 근거도 두껍다 — Lidar/AxCACHE, GMAC 통합 후
판정 수단, Q/P-Channel 전력 상태.

VIP 는 사라지지 않는다. **`검증할 수단이 없으면 만들기도 했습니다`** 로 위치가 뒤로 간다.
순서만 바뀌는데 면접 방어 가능성이 달라진다.

```text

[전 직장 퇴사 동기]
DV 팀에 속해 있지만 팀 운영상 설계를 겸하는 구조여서, 검증에 쌓이는 깊이는
제한적이었습니다. 검증 쪽으로 커리어를 깊이 쌓고 싶어 이직을 결심했습니다.

[지원 동기]
여러 IP가 통합된 시스템에서 검증되지 않은 지점을 찾아 테스트케이스를 만들고
디버깅하는 일을 해왔습니다. 인터커넥트와 부트, 전력 상태처럼 IP 하나만 봐서는 판단할
수 없는 구간을 다루다 보니, 시스템이 어떻게 맞물려 동작하는지를 넓게 보게 됐습니다.
무엇을 검증해야 하는지 정할 때 그 이해가 가장 많이 쓰였고, 통과한 결과에서 이상을
발견한 것도 거기서 나왔습니다.

검증할 수단이 없으면 만들기도 했습니다. 상용 VIP가 없는 프로토콜은 스펙만 보고
만들었고, IP를 통합한 뒤 설정대로 동작하는지 볼 수단이 없을 때는 스코어보드를
만들었습니다.

HBM base die에는 상용 VIP로 검증할 수 없는 Custom IP와 Analog-Digital Mixed IP가
많을 것입니다. 검증할 항목을 찾아내고 필요한 수단을 만들어 온 경험이 그
일에 도움이 될 것이라 생각합니다.

[향후 계획]
지금은 HBM JEDEC 표준을 공부하며 기본을 익히고 있습니다. 담당 IP를 스펙 단계부터 맡아
검증 항목을 세우고 커버리지로 닫으며, 뒤에 오는 과제에서 그대로 다시 쓸 수 있는
환경으로 남기고자 합니다. 3년 내에는 Mixed-Signal 검증까지 넓혀 회로 설계자와 같은
언어로 논의하는 검증 담당자가 되고자 합니다.
```

- 공백·줄바꿈 포함 **742자** / 1,000자 → 여유 258자

### 퇴사 동기에서 경력을 뺐다

이전 판은 `Lidar RTL 설계와 IP FE Sign-off도 맡았습니다` 로 과제를 늘어놨다. **퇴사 사유
칸에서 경력을 나열할 이유가 없다** — 그건 A 와 C 의 일이다. 설계 겸직은 `구조여서` 한
마디로 남기고 과제명은 뺐다.

### 그대로 지킨 선

- JD 인용(`담당 업무 네 가지 중 두 가지가…`)은 쓰지 않는다. 조건만 남겨도 어디를
  말하는지 전달된다
- `부족하지만`, 학벌·회사 규모 암시는 쓰지 않는다. 한계는 말하되 **다음 문장의 근거로**
  쓴다
- Coverage 는 향후 계획에서만 쓴다. 과거 성과로 쓰지 않는다
- C·D 와 어조를 맞춘다 — 목록·조직 설명 없이 서술체

---

## C. 자기소개서 2 — 경력기술서 (999자)  ★주력

**목록이 아니라 서사로.** 담당업무 칸이 업무를 촘촘히 나열하므로, 여기서 같은 항목을
다시 훑으면 1,500자 중 상당수를 중복에 쓴다. C 는 A 가 구조상 못 하는 것 —
**왜 그렇게 했는가, 무엇이 문제였는가, 결과가 무엇인가** — 만 담는다.

```text

[통과한 시뮬레이션에서 찾은 성능 결함]
Lidar 데이터 프로세서로 DDR에 접근하는 시뮬레이션은 통과했지만 소요 시간이 예상보다
길었습니다. 문제가 있다고 판단해 파형을 열어, CMN-700의 inbound 트래픽은
burst인데 outbound는 single로 나가는 것을 확인하고 AxCACHE 설정을 원인으로
특정했습니다. 제어 SFR을 추가해 outbound도 burst로 나가게
하니 소요 시간이 절반 이하로 줄었고, 같은 DDR 접근 경로를 가진 다른 서브시스템에도
반영되도록 전파했습니다. 이 건을 포함한 IP 통합·인터커넥트 검증 성과로 사내 베스트 엔지니어에
선정됐습니다.

[검증 수단을 직접 만든 일]
담당 서브시스템에 Q/P-Channel이 여러 개 있어, 채널마다 환경을 따로 만들면
같은 일이 반복되고 재사용도 어렵습니다. 스펙만 보고 In-house UVM VIP 하나로 일원화했습니다.
가장 오래 고민한 것은 무엇을 한 건의 트랜잭션으로 볼지였습니다. 이 프로토콜은 신호와
상태만 정의하고 그 단위는 정해 주지 않습니다. 아이템이 표현하지 못하는 현상은 검증이
볼 수 없으므로, 그 결정이 곧 설계였습니다. 채널이 늘어나도 이 VIP 하나로 검증할 수 있게 됐습니다.

Ethernet GMAC은 IP와 스펙이 모두 있었지만 SoC 통합 검증을 위한 수단이 필요해, UVM
환경을 구성하고 랜덤 패턴으로 프레임을 생성했습니다. 전송 전 랜덤화한 설정값을
보관했다가 실제 Tx/Rx 패킷과 descriptor를 파싱해 정상 동작 여부를 판단하는 스코어보드를
만들었습니다.

[파편화된 검증 환경을 하나로]
과제마다 담당자가 환경을 새로 만들어 IP 단위와 Top 환경이 파편화되어 있었습니다.
IP 단위의 env와 sequence를 Top-level에 그대로 얹는 수직 재사용 구조를 설계하고, GMAC
환경을 레퍼런스로 기준을 제시했습니다. 흩어져 있던 환경을 하나의 구조로 모았고,
현재 HPC 과제에서 DV 팀의 UVM 표준 구조로 쓰이고 있습니다.
```

- 공백 포함 **999자** / 1,000자 → 여유 1자

### 세 서사가 각각 다른 능력을 증명한다

| 서사 | 증명하는 것 |
|---|---|
| ① 검증할 수 있는 상태로 만드는 일 | **만든다** — Q/P-Channel VIP, GMAC 스코어보드 |
| ② 통과한 테스트에서 이상을 찾다 | **찾아낸다** (디버그) |
| ③ 검증 환경을 팀 표준으로 | **조직에 남긴다** |

### ①에 GMAC 을 합친 이유 — 앞선 판단을 뒤집는다

앞서 "GMAC 스코어보드는 VIP 와 같은 축이라 뺀다" 고 했는데, 그것은 **서사가 따로
있을 때** 의 이야기다. **한 서사 안에 두 사례를 넣으면 축이 겹치는 것이 아니라 같은
능력을 두 번 보인 것** 이 된다 — 반복이 아니라 보강이다.

그리고 합치니 더 나은 것이 나왔다. 두 사례가 **정반대** 다.

| | 있던 것 | 없던 것 | 만든 것 |
|---|---|---|---|
| Q-Channel | 스펙뿐 | 어휘(트랜잭션 정의) | seq_item 체계 |
| GMAC | IP · 스펙 | 판정 방법 | 스코어보드 · descriptor parser |

마지막 문장 `없는 것이 무엇이냐에 따라 만들어야 할 것이 달랐습니다` 가 둘을 묶는다.
사례 나열이 아니라 **판단 기준을 가진 사람**으로 읽힌다.

부수 효과로 구멍 하나가 메워졌다 — 직전 판까지 GMAC 스코어보드가 담당업무에만 있고
C 에는 없어서 **왜 만들었는지가 어디에도 없었다.**

### 각 서사에서 가장 중요한 한 줄

**①-1** `아이템이 표현하지 못하는 현상은 검증이 볼 수 없기 때문입니다.`
seq_item 설계를 왜 그렇게 고민했는지의 근거.

**①-2** `없는 것이 무엇이냐에 따라 만들어야 할 것이 달랐습니다.`
두 사례를 하나의 판단으로 묶는 문장.

**②** `판정은 통과였으므로 결과만 봤다면 넘어갔을 문제입니다.`
테스트가 통과했는데 이상을 느꼈다는 것 — pass/fail 만 보는 사람은 잡을 수 없다.

**③** `현재 진행 중인 HPC 과제에서 DV 팀의 UVM 표준 구조로 쓰이고 있습니다.`
인원수보다 "팀 표준" 이 낫다. 개인 산출물이 조직 자산이 됐다는 뜻이다.

### 규모에 대한 조정

`controller 와 device 양쪽 driver, monitor, agent, 커버리지 모델까지 구현했습니다`
문장을 뺐다. `~까지` 가 "이만큼이나 했다" 는 신호를 주는데, 신호 네 개짜리 프로토콜의
VIP 는 작은 것이 정상이다. **글이 크게 들렸다가 실물이 작아 보이는 낙차**를 만들 필요가
없다. 산출물 범위는 담당업무 칸의 `(agent·커버리지 모델)` 로 충분하다.

### 자기소개서 3(D)과의 분담

| 칸 | 담당하는 면 |
|---|---|
| 담당업무 | 사실 — 무엇을 만들었나 |
| **C ①** | **왜 그렇게 설계했나** (seq_item 이 커버리지 어휘를 정한다) |
| D | 지금 하고 있는 것 + 다른 설계 판단 (transition bin 대신 prev_state) |
| 경험/특허 | 프로젝트 개요 — **부담되면 여기를 비우는 것이 첫 조정 후보** |

**설계 판단을 C 와 D 가 서로 다른 것으로 든다.** 같은 판단을 두 번 쓰면 반복이 된다.

---

## D. 자기소개서 3 — 입사 가능 시기 및 기타사항 (917자)  ★논지=태도

**논지는 태도, USB 는 근거.** 부트 검증 자체는 HBM DV 에서 값이 큰 경험이 아니므로
사건은 태도를 뒷받침하는 자리까지만 내려온다. 태도 선언으로 여는 것은 원래 위험한
형태지만(흔한 자소서와 같은 출발), **바로 다음에 구체적인 사건이 오면 위험이 사라진다.**

```text

[입사 가능 시기]
현재 재직 중이며, 담당 과제 인수인계를 3주 내로 마치고 입사할 수 있습니다. 일정은
협의 가능합니다.

[배우고 때때로 익히면 또한 기쁘지 아니한가]
학이시습지 불역열호라는 말을 오래 마음에 두고 있습니다. 궁금한 것을 그냥 넘기지 못해
일과가 끝난 뒤에도 답을 찾을 때까지 붙들고 있을 때가 많았습니다. 배우는 데서 그치지 않고 직접 해봐야 제
것이 된다고 생각해 왔습니다.

신입 무렵 처음 맡은 IP가 USB 2.0이었습니다. Synopsys PHY 문서를 틈틈이 읽어 두었는데,
당장 필요해서가 아니라 제가 맡은 IP라 알고 싶었고 업무 중에 다 보지 못한 것은 따로
시간을 내어 읽었습니다. 뒤에 고객사에서 전달받은 Mask ROM 검증 코드를 시뮬레이션하다
에러가 났습니다. 코드도 IP도 저희가 만든 것이 아니어서 원인 분석은 고객사 쪽에서
진행하는 것이 통상이었습니다. 다만 파형에서 PHY와 link 사이 phyclock이 흐트러진 것이 눈에
걸렸고, 전원 관련 신호가 잘못 전달되면 PHY RTL 모델이 비정상 동작할 수 있다는 그 문서의
유의사항이 떠올랐습니다. 원인과 해결 방안을 전달했고, 고객사 크로스체크에서 원인이 맞다는 확인을 받았습니다.
그 문서를 읽어 두지 않았다면 원인을 짚지 못했을 것입니다.

[지시를 기다리지 않은 일]
더 낫게 만들 여지가 보이면 지시를 기다리지 않았습니다. Dynamic Test Loading 방법론을
검증 환경에 적용해 시뮬레이션 시간을 줄인 것도, DV 팀 공용 UVM 환경의 구조를 정리한
것도 과제로 잡혀 있던 일은 아니었습니다. 혼자 하기 어려운 일은 인원을 꾸려 진행을
맡았고, 사내 AI Transformation TF에 SoC 그룹 대표로 참여하고 있는 것도 그 연장입니다.
HBM base die 검증에서도 같은 방식으로 일하고 싶습니다.
```

- 공백·줄바꿈 포함 **917자** / 1,000자 → 여유 83자

### 몰입·끈기를 넣은 방식

원하는 인상은 *열정 있고 엉덩이 무거운 사람*. **시간이 아니라 동기와 사실로** 쓴다.

| 넣은 문장 | 무엇을 증명하나 |
|---|---|
| `궁금한 것을 남겨 두면 잘 넘어가지 못해, 일과가 끝난 뒤에도 붙들고 있을 때가 많았습니다` | 인용의 뒷부분(기쁘지 아니한가)을 채운다. **원인이 궁금증**이라 근태가 아니라 몰입으로 읽힌다 |
| `당장 필요해서가 아니라 제가 맡은 IP라 알고 싶었고` | 시켜서가 아니라는 동기 |
| `업무 중에 다 보지 못한 것은 따로 시간을 내어 읽었습니다` | **자기 시간을 썼다** — 야근이 아니라 자기계발이라 근태 신호가 없다 |
| `불편한 것이 보이면 같은 고민을 한 사람이 이미 있고 해결책도 나와 있는 경우가 많았습니다` | 주도성을 **겸손하게** 세운다. 주체가 내 안목이 아니라 찾아보는 습관이라 오만하게 읽히지 않는다 |

**야근·시간 표현은 쓰지 않는다.** *시간 가는 줄 모르고* 는 상투구라 앞뒤 문장까지 같은
급으로 끌어내리고, 심사자에 따라 `효율이 낮다` / `관리 부담이 있다` 로도 읽힌다.
얻는 것(열심히 하는 사람)보다 잃는 것이 크다. **같은 재료를 서류에서는 동기로, 면접에서는
시간으로** 말하면 된다 — 면접은 사람이 맥락을 보므로 몰입으로 전달될 여지가 크다.

### 사실 정정

- **Q-Channel VIP 는 과제 기간 내 업무다.** 과제 밖에서 한 일로 쓰지 않는다
- **과제 일정 밖에서 한 것은 UVM 표준화와 DTL.** 손이 비는 때에 직접 찾아보고 계획해
  만들어 둔 것이므로, 세 번째 문단의 근거는 이 둘로 맞췄다
- 그래서 세 번째 문단에서 Q/P-Channel VIP 를 뺐다. C 첫 서사와 중복이기도 했다

### 그대로 지킨 선

- "팀장들이 좋아한다" 는 쓰지 않는다 — 검증 불가능한 문장은 신뢰를 깎는다
- IP 버그를 찾았다고 쓰지 않는다 — PHY RTL 모델 오동작이지 IP 결함이 아니다
- RTL 설계·Sign-off 는 넣지 않는다 — 디자인하우스라는 회사 형태 때문이지 자원한 일이
  아니므로 적극성의 근거가 되지 않는다

---

## E. 경험/특허 (선택, 263자)  ★AX T/F 로 교체

```text
회사(또는 소속조직)명: 사내 AI Transformation TF
활동기간: 2026-06 ~ 진행 중
활동내용:
SoC 그룹(SW·DV·Platform·설계 4팀)을 대표해 참여하고 있습니다. 그룹 내 AI 활용 과제를
관리하고, DV·RTL·SW 각 팀에 필요한 것을 정리해 회의에 제안하고, 전사 도입으로
이어지도록 하고 있습니다.
사내 데이터를 외부로 내보낼 수 없는 제약이 있어, 자체 환경에서 구동할 수 있는 오픈
웨이트 모델을 검토하는 것부터 시작했습니다.
```

- 공백·줄바꿈 포함 **263자** / 500자 → 여유 237자
- **`개인 프로젝트`로 쓰여 있던 Q-Channel VIP 항목을 뺐다.** A·C·D 에서는 담당 과제
  업무로 쓰고 있어 **서류 안에서 사실이 충돌**했다(개인 포트폴리오 방침을 바꾼 뒤 E 만
  갱신되지 않았다). VIP 는 이미 세 곳에 있으므로 잃는 것이 없다.
- E 의 하위 필드가 `소속조직명 / 활동기간 / 활동내용` 이므로 **기간이 있는 활동**을 쓰는
  칸이다. 수상은 활동이 아니라 결과여서 이 칸에 맞지 않는다 → **D 로 보냈다.**
- 2026-06 시작이라 두 달째다. **성과는 쓰지 않고 역할만** 쓴다. 활동기간을 보면 심사자가
  초기임을 알고 읽는다.
- **오픈 웨이트 모델 검토는 넣되 모델명은 쓰지 않는다.** 사내 데이터를 외부로 낼 수 없는
  제약은 이 조직도 같으므로, "AI 를 써봤다"가 아니라 **제약 안에서 도입 경로를 찾는 일**
  로 읽힌다. 특정 모델명은 내부 검토 사항이고 몇 달이면 낡는다.
- 경험/특허는 **반복 추가가 가능**하다(`경험 추가`). 더 쓸 활동이 생기면 항목을 늘린다.

---

## F. 어학 / 외국어 능력 (그대로 입력)

| 항목 | 값 |
|---|---|
| 언어 / 시험종류 | 영어 / OPIc |
| 점수 | Intermediate Mid 1 |
| 취득일 | 2026-06-06 |
| 등록번호 | 2A6656114699 |
| 회화 / 작문 / 독해 | Intermediate / Intermediate / Advanced |

---

## G. 제출 전 체크리스트

- [ ] 부서명 (폼 필수)
- [ ] 급여 (폼 필수, 만원)
- [ ] 재직중 `근무종료년월` 입력 방식
- [ ] C의 `[TODO]` 1개 — UVM 표준 적용 범위
- [ ] D·E의 `[TODO]` — 입사 가능 주수, 거주지/이전 계획, 프로젝트 착수 월
- [ ] 담당업무 후보 1 / 2 택일
- [ ] 고객사 AMBA VIP — 구체적으로 무엇을 고쳤는지 (문장 교체용)
- [ ] D와 E 내용 중복 조절
- [ ] 글자수 폼에서 재확인 (공백 포함/제외 기준이 다를 수 있음)

---

## H. 내일 CRMU 적용 결과에 따라 바뀌는 것

`q_chan_vip` 을 사내 CRMU requester / CPU responder 인터페이스에 bind 해서 돌아가면,
지원서의 무게중심이 이동합니다.

**성공 시 바뀔 것**

- C ①블록의 Q-Channel 문장이 `개인 과제로` → **사내 과제**로 승격. 이 프레임에서 가장
  강한 사례가 됩니다: *설계가 완성되기 전에 검증 환경을 먼저 세워 투입했다.*
- 담당업무 칸(A)에 Q-Channel 항목이 들어갈 수 있게 됩니다 (지금은 개인 과제라 제외).
- D의 "지금 만들고 있는 것" 서술이 실행 결과를 포함하는 형태로 바뀝니다.
- E(경험/특허) 칸이 불필요해질 수 있습니다.

**순서 주의**

되고 나서 씁니다. 미리 써두고 안 붙으면 되돌려야 합니다. 신호 네이밍, 클럭 도메인,
리셋 시 QREQn 초기값(`cfg.reset_qreqn_high`), active-low 규약에서 시간이 걸릴 수 있습니다.

**첫 컴파일에서 걸릴 만한 곳** (이 코드는 한 번도 컴파일된 적이 없습니다)

- `covergroup ... with function sample(...)` — 막히면 아이템을 클래스 멤버에 저장하고
  인자 없는 `sample()` 로 바꾸는 우회
- `coverpoint ... iff (...)` — guard 가 sample 인자를 참조. 막히면 guard 를 `write()` 의
  `if` 로 옮기고 covergroup 을 쪼개기
- `bins long = {[32:$]}` — `int unsigned` 에 `$` 사용, 경고 가능
- `get_inst_coverage()` — 커버리지 수집 옵션 없이 컴파일하면 0. 테스트는 이미 에러
  조건으로 쓰지 않음
- 컴파일 순서: `qch_if.sv` → `qch_items.sv` → `qch_agent_pkg.sv` → 테스트

**공개 저장소 오염 주의**

`khsda50/With-Claude` 는 **public** 입니다. 실제 신호 이름·인터페이스 구조·config 값·
파형·커버리지 결과가 push 되면 되돌려도 git 이력에 남습니다. 회사 PC 에서 이 저장소를
clone 하지 말고 **파일만 복사**해 가시면 실수로 push 할 경로 자체가 없습니다.

---

## I. 담당업무 재구성 방향 (작업 대기)

**문제**: 현재 담당업무 9줄 중 6줄이 "환경/인프라 구축". JD 담당업무 1번
("IP 및 Mixed 설계 검증")에 대응하는 서술이 비어, 도구 만든 사람으로만 읽힌다.

**빠진 어휘** — JD가 쓰는 검증 단어가 칸에 거의 없다:

| JD 어휘 | 현재 |
|---|---|
| 검증 계획 수립 | 없음 |
| Test Case 개발 | 없음 |
| Regression Test | 없음 |
| 디버그 / 이슈 규명 | SAM 라우팅 1개 |

**새 블록 순서** (문장은 미작성):

```
[IP/서브시스템 검증]        ← 신설·최상단
- 다종 IP 검증계획 수립 → 테스트케이스 개발 → 회귀 운영
- 인터커넥트 데이터패스 검증, SAM 라우팅 이슈 규명
- 부트 시나리오 검증 (Mask ROM)
- AxCACHE 성능 이슈 발견·해결   ← C와 층위 분리해 짧게만

[검증 환경·VIP]             ← 기존 1블록을 2번째로
[방법론·인프라]             ← 축소
[RTL 설계·Sign-off]         ← 2줄 → 1줄
```

- 예산 확보: 설계·Sign-off와 방법론에서 줄여 검증 블록 자리를 만든다.
  검증 직무 지원서에서 설계 이력이 2줄까지 갈 필요는 없다.
- C(경력기술서) ①블록 제목도 `스펙 기반 IP 검증 환경 구축` →
  `IP 검증과 검증 환경 구축` 으로.

**Q-Channel 서술 주의**

사내 적용은 bind 연동 구조 구성 + 예제 시나리오 동작 확인까지 진행됨.
"검증 완료"로 쓰면 면접에서 커버리지·발견 이슈를 반드시 되묻는다. 권장 표현:

> 사내 검증 환경에 bind 연동 구조를 구성하고 예제 시나리오로 동작 확인

사실 그대로이고 세기도 크게 다르지 않다.

### I-2. 반영할 신규 정보 (2026-08-11 확인)

**Test case 개발 실적** — "검증한 티"를 만드는 핵심. 셋 다 고유명사가 있어 강하다.

| 대상 | 내용 | 상태 |
|---|---|---|
| Cortex-A55 CPU 클러스터 | DSU 기능 분석 → 스펙 지원 feature 기준 케이스 개발 | 진행 중 |
| Ethernet GMAC (DPI) | ECC·timestamp 등 기능 케이스 | 완료 |
| Lidar 서브시스템 (DPI) | integration 관점 검증 | 완료 |
| UVM 기반 GMAC 환경 | constrained-random + **스코어보드 + descriptor parser** | 완료 |

**GMAC 을 두 방법론으로 검증한 것이 이 표의 숨은 자산이다.** DPI 환경에서는
ECC·timestamp directed 케이스, UVM 환경에서는 constraint 기반 random. 한 줄에 같이
쓰면 "랜덤 좀 써봤다" 가 아니라 **directed 와 constrained-random 을 다 써봤고 어디에
무엇이 맞는지 안다** 로 읽힌다.

DSU 항목이 특히 세다. "클러스터 내부를 다 보진 않지만 DSU 기능을 파악해 자사 스펙이
지원하는 feature를 케이스로 만든다" 는 **스펙 기반 검증계획 수립**(JD 자격요건 2번)의
실물 증거다.

**VIP 프레이밍** — 사용자 제안 채택:

> SoC DV였지만 그 안에서 필요한 VIP를 직접 만들어 썼다

직무 전환 논리를 한 문장으로 해결한다. 과제 적용 여부를 항목마다 밝힐 필요는 없다.
**Q-Channel 방침 변경 (2026-08-11)**: 개인 과제로 두지 않고 **과제에 적용**한다.
규모가 작아 부담이 적다는 판단. 적용되면 담당업무 칸에 넣을 수 있고, C ①블록의
`개인 과제로` 는 사내 과제로 승격된다.

- 현재 진행 상태: bind 연동 구조 구성 + 예제 시나리오 동작 확인
- **제출 시점 기준으로 쓴다.** 적용이 끝나 있으면 `사내 CRMU 검증에 적용`,
  아직이면 `bind 연동 구성 및 동작 확인` — 후자도 충분히 강하다.
- GMAC·USB·AMBA VIP 만으로도 "필요한 VIP를 직접 만들어 썼다" 는 이미 성립하므로,
  Q-Channel 이 늦어도 프레임은 무너지지 않는다.

**IP 간 동작 검증 — 앞선 판단 수정**

SoC 통합을 "주변부" 로 밀라고 했던 것을 되돌린다. JD 조직 소개에
`Digital IP 및 Subsystem/Top Level 검증` 이 명시돼 있고, HBM base die 도 in-house IP 를
붙인 구조라 IP 간 연동 검증이 그대로 필요하다. 순서만 IP 먼저, Top 나중.

**새 블록 구성**

```
[IP 통합·서브시스템 검증]                        ← 신설·최상단
- Cortex-A55 CPU 클러스터 검증: DSU 기능 분석 →
  스펙 지원 feature 기준 테스트케이스 개발 (진행 중)
- Ethernet GMAC: ECC·timestamp 기능 케이스(directed) +
  UVM 환경 constraint 기반 random 패턴 검증
- Lidar 서브시스템 integration 검증
- 인터커넥트 데이터패스·SAM 라우팅 이슈 규명, AxCACHE 성능 이슈 발견·해결

[검증 환경·VIP]
- SoC 검증에 필요한 VIP를 직접 제작 — GMAC UVM 환경, USB 2.0 IP 단위 환경
- SV class 기반 고객사 AMBA VIP 인수·구조 파악·확장
- DPI 기반 전사 IP Unit sim 환경 구축·운영

[방법론·인프라]                             ← 1~2줄로 축소
[RTL 설계·Sign-off]                        ← 1줄로 축소
```

예산: 새 블록이 들어가면 500자 초과. 설계·Sign-off 를 1줄로 줄이고 방법론에서
Coverage DB·GLS 를 덜어내야 맞는다. 실측은 다음 작업.

### I-3. 추가 확인 (2026-08-11) — 판정 축과 Q-Channel 의 진짜 동기

**① GMAC UVM 스코어보드 — 지금까지 지원서에 없던 최대 누락**

| 층 | 내용 |
|---|---|
| 랜덤화 | frame format(일반/jumbo), payload memory data pattern, 수반 data size |
| 예측 | 전송 전 랜덤화된 레지스터 설정값(frame type, length, pattern)을 버퍼에 저장 |
| 비교 | TX/RX 패킷 도착 시 기대값과 실제값 대조 |
| 파싱 | memory interface **descriptor parser** 를 IP 스펙에 맞게 구현 |

predictor + comparator 구조의 정식 스코어보드다. descriptor parser 는
**스펙을 읽고 스펙 준수 파서를 구현한 것** 이라 JD 자격요건 2번의 직접 증거.
`constrained-random 자극 → 스코어보드 판정 → 스펙 기반 파싱` 이 한 벌로 성립한다.

**담당업무 칸 반영 (필수)**

```
- Ethernet GMAC UVM 환경: frame format·payload 랜덤화, 설정값 대 실제 패킷 비교
  스코어보드 및 IP 스펙 기반 memory descriptor parser 구현
```

**② CPU 서브시스템도 UVM 기반이고, FW 기반 검증도 UVM TB 위에서 한다**

- Cortex-A55 클러스터 검증(DSU feature 케이스)이 UVM 환경에서 진행 중
- **FW 기반 검증을 UVM 테스트벤치에서 수행** ← HW/SW 경계를 다 다룬다는 신호.
  HBM base die 도 test firmware 로 구동하는 영역이 있어 문맥이 이어진다

**③ Q-Channel VIP 의 동기가 바뀐다 — 개인 공부가 아니라 과제 요구**

CPU 서브시스템 안에 **Q-Channel / P-Channel 이 다수** 존재해, 채널마다 따로 검증하는
대신 **일원화·재사용 가능한 In-house VIP** 를 만들어 대응하려는 것.

이 사실이 지원서 전체에서 가장 큰 변화다.

- 지금까지 서술: "직무 준비를 위해 개인적으로 만들어 본 agent"
- 실제: **"다수 채널을 재사용 가능한 하나의 VIP 로 일원화하려는 과제 대응"**
- JD 담당업무 2번(In-house VIP 개발)·자격요건 3번(신규 Protocol UVM Agent 설계)에
  정면으로 걸리고, **동기가 실무 문제 해결이라 설득력이 다르다**
- 만들어 둔 `qch_multi_env` / virtual sequencer 도 곧 "채널이 많다" 는 실제 요구에
  대한 답이 된다. 우연이 아니라 요구에서 나온 구조로 설명할 수 있다

→ C ①블록의 `개인 과제로` 를 삭제하고, 담당업무 칸에도 아래 형태로 넣는다.

```
- 서브시스템 내 다수 Q-Channel/P-Channel 검증을 일원화하기 위한
  In-house UVM VIP 개발 (스펙 기반 A-to-Z)
```

**④ 남은 확인 항목**

- [ ] 스코어보드가 실제로 잡은 버그 사례 (있으면 면접 최고 재료)
- [ ] DSU 에서 검증한 feature 목록, cluster power state 포함 여부
- [ ] FW 기반 검증에서 FW 를 직접 작성했는지
